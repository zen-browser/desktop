name: Windows Release Build

permissions:
  contents: read

on:
  workflow_call:
    inputs:
      generate-gpo:
        required: true
        type: boolean
        default: false
      build-version:
        description: "The version to build"
        required: true
        type: string
      profile-data-path-archive:
        description: "The path to the zip archive containing the profile data"
        type: string
      release-branch:
        description: "The branch to build"
        required: true
        type: string
      MOZ_BUILD_DATE:
        type: string
        required: true
        default: ""
      use-sccache:
        description: "Use sccache"
        required: true
        type: boolean
        default: false

jobs:
  windows-build:
    name: Build Windows - ${{ matrix.arch }}
    # aarch64 does not need full 16x, and we also dont use full LTO when generating GPO
    runs-on: windows-latest
    env:
      SCCACHE_GHA_ENABLED: ${{ inputs['use-sccache'] && 'true' || 'false' }}
      CARGO_TERM_COLOR: always
      RUSTC_WRAPPER: ${{ inputs['use-sccache'] && 'sccache' || '' }}
      CARGO_INCREMENTAL: 0
    strategy:
      fail-fast: false
      matrix:
        arch: [x86_64, aarch64]

    steps:
      - name: Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@main
        if: ${{ runner.os == 'Linux' && !(inputs['generate-gpo'] && matrix.arch == 'aarch64') }}
        with:
          tool-cache: false

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version-file: ".nvmrc"

      - name: Run sccache-cache
        uses: mozilla-actions/sccache-action@main
        if: ${{ inputs['use-sccache'] }}
        with:
          disable_annotations: true

      - name: Expose actions cache variables
        uses: actions/github-script@v6
        with:
          script: |
            core.exportVariable('ACTIONS_CACHE_URL', process.env['ACTIONS_CACHE_URL'])
            core.exportVariable('ACTIONS_RUNTIME_TOKEN', process.env['ACTIONS_RUNTIME_TOKEN'])

      - name: Setup Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install dependencies
        run: |
          npm ci

      - name: Load Surfer CI setup
        run: npm run surfer -- ci --brand ${{ inputs['release-branch'] }} --display-version ${{ inputs['build-version'] }}

      - name: Download Firefox and dependencies
        if: ${{ !(inputs['generate-gpo'] && matrix.arch == 'aarch64') }}
        run: npm run download

      - name: win-cross Cache
        env:
          SEGMENT_DOWNLOAD_TIMEOUT_MINS: 5
        id: cache-win-cross
        uses: actions/cache@v5
        with:
          path: ${HOME}/win-cross
          key: win-cross

      - name: Install MozillaBuild
        shell: pwsh
        run: |
          Invoke-WebRequest -Uri "https://ftp.mozilla.org/pub/mozilla/libraries/win32/MozillaBuildSetup-Latest.exe" -OutFile "MozillaBuildSetup.exe"
          Start-Process -FilePath "MozillaBuildSetup.exe" -ArgumentList "/S" -Wait
          $env:MOZILLABUILD = "C:\mozilla-build"
          echo "MOZILLABUILD=C:\mozilla-build" >> $env:GITHUB_ENV

      - name: Setup for Windows
        if: steps.cache-win-cross.outputs.cache-hit != 'true' && !(inputs['generate-gpo'] && matrix.arch == 'aarch64')
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path "$HOME/win-cross" | Out-Null
          Set-Location engine
          echo Setup wine
          Invoke-WebRequest "https://firefox-ci-tc.services.mozilla.com/api/index/v1/task/gecko.cache.level-1.toolchains.v3.linux64-wine.latest/artifacts/public%2Fbuild%2Fwine.tar.zst" -OutFile "wine.tar.zst"
          tar -xf "wine.tar.zst" -C "$HOME/win-cross"
          Remove-Item "wine.tar.zst" -Force
          echo Setup Visual Studio
          python mach python --virtualenv build taskcluster/scripts/misc/get_vs.py build/vs/vs2026.yaml "$HOME/win-cross/vs2026"

      - name: Import
        if: ${{ !(inputs['generate-gpo'] && matrix.arch == 'aarch64') }}
        env:
          SURFER_COMPAT: ${{ matrix.arch }}
        shell: pwsh
        run: |
          $env:PATH = "$env:USERPROFILE\.cargo\bin;$env:PATH"
          npm run import -- --verbose

      - name: Bootstrap
        if: ${{ !(inputs['generate-gpo'] && matrix.arch == 'aarch64') }}
        run: |
          set -x
          cd engine/
          chmod -R +x "$(echo ~)/win-cross/vs2026" || true
          cd ..
          export SURFER_PLATFORM="win32"
          npm run bootstrap
          cd engine/
          ls ~/.mozbuild/clang/lib/clang/
          echo "export LIB=\"$(cd ~/.mozbuild/clang/lib/clang/* && cd lib/windows && pwd)\"" >> ../configs/common/mozconfig
          cat ../configs/common/mozconfig
          ls ~/win-cross/wine || true
          ls ~/win-cross/vs2026 || true

      - name: Setup Rust
        if: ${{ !(inputs['generate-gpo'] && matrix.arch == 'aarch64') }}
        shell: pwsh
        run: |
          $toolchain = (Get-Content .rust-toolchain -Raw).Trim()
          Invoke-WebRequest "https://win.rustup.rs/x86_64" -OutFile "rustup-init.exe"
          .\rustup-init.exe -y --default-toolchain $toolchain
          $env:Path = "$HOME\.cargo\bin;$env:Path"
          if ("${{ matrix.arch }}" -eq "aarch64") {
            rustup target add aarch64-pc-windows-msvc
          } else {
            rustup target add x86_64-pc-windows-msvc
          }
          # Stored in build/windows/.windows-rs-version
          $WINDOWS_RS_VERSION = (Get-Content build/windows/.windows-rs-version -Raw).Trim()
          Set-Location engine
          cargo install cargo-download --locked
          cargo download -x windows=$WINDOWS_RS_VERSION
          $env:CARGO_INCREMENTAL = "0"
          Add-Content ..\configs\common\mozconfig ""
          Add-Content ..\configs\common\mozconfig "export MOZ_WINDOWS_RS_DIR=$(Get-Location)/windows-$WINDOWS_RS_VERSION"

      - name: Build language packs
        if: ${{ !(inputs['generate-gpo'] && matrix.arch == 'aarch64') }}
        run: sh scripts/download-language-packs.sh

      - name: Download artifact (if use profdata)
        uses: actions/download-artifact@v4
        if: ${{ !inputs['generate-gpo'] && matrix.arch == 'x86_64' }}
        with:
          path: ~/artifact
          name: windows-profdata-${{ matrix.arch == 'aarch64' && 'arm64' || matrix.arch }}

      - name: Show artifact info
        if: ${{ !inputs['generate-gpo'] && matrix.arch == 'x86_64' }}
        run: |
          ls ~/artifact
          ls ~/artifact/en-US.log
          ls ~/artifact/merged.profdata
          chmod +x ~/artifact/en-US.log
          chmod +x ~/artifact/merged.profdata

      - name: Insert API Keys
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path "$HOME/.zen-keys" | Out-Null
          $key = "${{ secrets.ZEN_SAFEBROWSING_API_KEY }}"
          if ([string]::IsNullOrWhiteSpace($key)) { $key = "placeholder" }
          Set-Content -Path "$HOME/.zen-keys/safebrowsing.dat" -Value $key

      - name: Build
        if: ${{ !(inputs['generate-gpo'] && matrix.arch == 'aarch64') }}
        env:
          SURFER_COMPAT: ${{ matrix.arch }}
          ZEN_RELEASE_BRANCH: ${{ inputs['release-branch'] }}
        shell: pwsh
        run: |
          $env:SURFER_PLATFORM = "win32"
          $env:ZEN_CROSS_COMPILING = "1"
          if ("${{ inputs['generate-gpo'] }}" -eq "true") {
            $env:ZEN_GA_GENERATE_PROFILE = "1"
          }
          if ("${{ inputs.MOZ_BUILD_DATE }}" -ne "") {
            $env:MOZ_BUILD_DATE = "${{ inputs.MOZ_BUILD_DATE }}"
          }
          bash .github/workflows/src/release-build.sh

      - name: Package
        if: ${{ !(inputs['generate-gpo'] && matrix.arch == 'aarch64') }}
        env:
          SURFER_COMPAT: ${{ matrix.arch }}
          ZEN_GA_DISABLE_PGO: true
        run: |
          set -x
          export SURFER_PLATFORM="win32"
          export ZEN_CROSS_COMPILING=1
          export ZEN_RELEASE=1
          npm run package
          mv ./dist/zen-$(npm run --silent surfer -- get version | xargs).en-US.win64${{ matrix.arch == 'aarch64' && '-aarch64' || '' }}.zip zen.win64.zip
          ls ./dist
          ls .

      - name: Remove API Keys
        run: |
          Remove-Item -Recurse -Force "$env:USERPROFILE\.zen-keys" -ErrorAction SilentlyContinue

      - name: Move package for PGO upload
        if: ${{ inputs['generate-gpo'] && matrix.arch == 'x86_64' }}
        run: |
          set -x
          mv ./zen.win64.zip ./zen.win64-pgo-stage-1.zip

      - name: Rename artifacts
        if: ${{ !inputs['generate-gpo'] }}
        run: |
          mv ./zen.win64.zip zen.win-${{ matrix.arch == 'aarch64' && 'arm64' || matrix.arch }}.zip
          mv ./dist/output.mar windows${{ matrix.arch == 'aarch64' && '-arm64' || '' }}.mar
          mv ./dist/zen.installer.exe ./zen.installer${{ matrix.arch == 'aarch64' && '-arm64' || '' }}.exe

      - name: Upload artifact (PGO)
        uses: actions/upload-artifact@v4
        if: ${{ inputs['generate-gpo'] && matrix.arch == 'x86_64' }}
        with:
          retention-days: 5
          name: ${{ matrix.arch == 'aarch64' && 'arm64' || matrix.arch }}-${{ inputs['profile-data-path-archive'] }}
          path: ./zen.win64-pgo-stage-1.zip

      - name: Remove unnecessary files from obj
        if: ${{ !inputs['generate-gpo'] }}
        run: |
          set -x
          mkdir obj-${{ matrix.arch }}-pc-windows-msvc/

          Remove-Item -Recurse -Force "engine/obj-${{ matrix.arch }}-pc-windows-msvc/_tests" -ErrorAction SilentlyContinue
          Remove-Item -Recurse -Force "engine/obj-${{ matrix.arch }}-pc-windows-msvc/config/autoconf.mk" -ErrorAction SilentlyContinue
          Remove-Item -Recurse -Force "engine/obj-${{ matrix.arch }}-pc-windows-msvc/config.statusd/substs/" -ErrorAction SilentlyContinue
          Remove-Item -Recurse -Force "engine/obj-${{ matrix.arch }}-pc-windows-msvc/dist/install" -ErrorAction SilentlyContinue

          find engine/obj-${{ matrix.arch }}-pc-windows-msvc/dist -maxdepth 1 -type f -name "*.zip" -delete

          cp -r --no-dereference engine/obj-${{ matrix.arch }}-pc-windows-msvc/* obj-${{ matrix.arch }}-pc-windows-msvc/ || true

      - name: Upload dist object
        if: ${{ !inputs['generate-gpo'] }}
        uses: actions/upload-artifact@v4
        with:
          retention-days: 2
          name: windows-x64-obj-${{ matrix.arch == 'aarch64' && 'arm64' || matrix.arch }}
          path: obj-${{ matrix.arch }}-pc-windows-msvc

      - name: Upload artifact (if Twilight branch, binary)
        if: ${{ inputs['release-branch'] == 'twilight' && !inputs['generate-gpo'] }}
        uses: actions/upload-artifact@v4
        with:
          retention-days: 5
          name: zen.win-${{ matrix.arch == 'aarch64' && 'arm64' || matrix.arch }}.zip
          path: ./zen.win-${{ matrix.arch == 'aarch64' && 'arm64' || matrix.arch }}.zip

      - name: Upload artifact (if Twilight branch, installer)
        if: ${{ inputs['release-branch'] == 'twilight' && !inputs['generate-gpo'] }}
        uses: actions/upload-artifact@v4
        with:
          retention-days: 5
          name: zen.installer${{ matrix.arch == 'aarch64' && '-arm64' ||  '' }}.exe
          path: ./zen.installer${{ matrix.arch == 'aarch64' && '-arm64' || '' }}.exe

      - name: Upload artifact (if Twilight branch, .mar)
        if: ${{ inputs['release-branch'] == 'twilight' && !inputs['generate-gpo'] }}
        uses: actions/upload-artifact@v4
        with:
          retention-days: 5
          name: windows${{ matrix.arch == 'aarch64' && '-arm64' || '' }}.mar
          path: ./windows${{ matrix.arch == 'aarch64' && '-arm64' || '' }}.mar

      - name: Upload artifact (if Twilight branch, update manifests)
        if: ${{ inputs['release-branch'] == 'twilight' && !inputs['generate-gpo'] }}
        uses: actions/upload-artifact@v4
        with:
          retention-days: 5
          name: windows_update_manifest_${{ matrix.arch == 'aarch64' && 'arm64' || matrix.arch }}
          path: ./dist/update
