diff --git a/browser/components/sidebar/SidebarState.sys.mjs b/browser/components/sidebar/SidebarState.sys.mjs
--- a/browser/components/sidebar/SidebarState.sys.mjs
+++ b/browser/components/sidebar/SidebarState.sys.mjs
@@ -11,6 +11,12 @@ XPCOMUtils.defineLazyPreferenceGetter(
   "verticalTabsEnabled",
   "sidebar.verticalTabs"
 );
+XPCOMUtils.defineLazyPreferenceGetter(
+  lazy,
+  "zenVerticalTabsEnabled",
+  "zen.tabs.vertical",
+  true
+);
 
 const DEFAULT_LAUNCHER_VISIBLE = false;
 
@@ -500,7 +506,11 @@ export class SidebarState {
     const boxEl = this.#controller._box;
     const contentAreaEl =
       this.#controllerGlobal.document.getElementById("tabbrowser-tabbox");
-    tabContainer.toggleAttribute("expanded", expanded);
+    // Zen owns #tabbrowser-tabs[expanded] with zen-sidebar-expanded.
+    // Skip Firefox launcherExpanded writes when Zen vertical tabs are enabled.
+    if (!lazy.zenVerticalTabsEnabled) {
+      tabContainer.toggleAttribute("expanded", expanded);
+    }
     if (mainEl?.toggleAttribute) {
       mainEl.toggleAttribute("sidebar-launcher-expanded", expanded);
     }
